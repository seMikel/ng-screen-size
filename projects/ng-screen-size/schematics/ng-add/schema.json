{
  "$schema": "http://json-schema.org/schema",
  "$id": "SchematicsAdd",
  "title": "Add Schema",
  "type": "object",
  "properties": {
    "project": {
      "type": "string",
      "description": "The name of the project.",
      "$default": {
        "$source": "projectName"
      }
    },
    "path": {
      "type": "string",
      "format": "path",
      "description": "The path to create the screen-size scss config."
    }
  }
}